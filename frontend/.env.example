# URL base de API Gateway (o de `sam local start-api`, ej. http://127.0.0.1:3000)
VITE_API_URL=http://127.0.0.1:3000

# Pendiente de Fase 6 (Cognito) — sin User Pool todavía, el frontend no hace login real.
# VITE_COGNITO_USER_POOL_ID=
# VITE_COGNITO_CLIENT_ID=
# VITE_COGNITO_DOMAIN=
